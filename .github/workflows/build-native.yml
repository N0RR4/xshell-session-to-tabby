name: Build Windows Native Exe

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      - name: Download and Extract GraalVM
        run: |
          $url = "https://github.com/graalvm/graalvm-ce-builds/releases/download/jdk-21.0.2/graalvm-community-jdk-21.0.2_windows-x64_bin.zip"
          curl -L -o graalvm.zip $url
          Expand-Archive -Path graalvm.zip -DestinationPath C:\ -Force
          Remove-Item graalvm.zip

      - name: Set GRAALVM_DIR
        run: |
          $gdir = (Get-ChildItem C:\graalvm* | Select-Object -First 1).FullName
          echo "GRAALVM_DIR=$gdir" | Out-File -FilePath $env:GITHUB_ENV -Encoding utf8 -Append

      - name: Install native-image
        run: |
          Write-Host "GRAALVM_DIR=$env:GRAALVM_DIR"
          Write-Host "Contents:"
          Get-ChildItem "$env:GRAALVM_DIR\bin\gu.exe" -ErrorAction SilentlyContinue
          Get-ChildItem "$env:GRAALVM_DIR\bin\gu.cmd" -ErrorAction SilentlyContinue
          Get-ChildItem "$env:GRAALVM_DIR" -ErrorAction SilentlyContinue | Select-Object Name
          & "$env:GRAALVM_DIR\bin\gu.cmd" install native-image --no-verify

      - name: Build JAR
        run: mvn package -DskipTests -q

      - name: Build Native Image
        run: |
          & "$env:GRAALVM_DIR\bin\native-image.cmd" -cp target/xshell-session-to-tabby.jar -H:Name=xshell-session-to-tabby --no-fallback -H:ConfigurationFileDirectories=src/main/resources/native/agent-output com.coffee.Main
        shell: cmd

      - name: Upload exe
        uses: actions/upload-artifact@v4
        with:
          name: xshell-session-to-tabby-exe
          path: xshell-session-to-tabby.exe
